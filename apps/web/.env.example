NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# Optional. BaseScan block explorer base URL used to build on-chain tx links.
# Defaults to Base Sepolia (https://sepolia.basescan.org) where the treasury
# guard contract is currently deployed. Set to https://basescan.org for Base
# mainnet.
NEXT_PUBLIC_BASE_SCAN_BASE_URL=
