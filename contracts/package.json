{
  "name": "treasury-sentinel-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:mock-usdc:base-sepolia": "hardhat run scripts/deploy-mock-usdc.js --network baseSepolia",
    "fund:guard:base-sepolia": "hardhat run scripts/fund-guard.js --network baseSepolia",
    "seed:recipient:base-sepolia": "hardhat run scripts/seed-recipient.js --network baseSepolia",
    "demo:local": "hardhat run scripts/local-payment-demo.js",
    "demo:localhost": "hardhat run scripts/local-payment-demo.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.0",
    "hardhat": "^2.28.0"
  }
}
